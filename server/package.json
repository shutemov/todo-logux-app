{
  "name": "server",
  "version": "1.0.0",
  "description": "1. Move to ```cd ~/server ```\r 2. Execute ``` npm install ```\r 3. Execute ``` node index ```",
  "main": "index.js",
  "dependencies": {
    "@logux/server": "^0.6.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
